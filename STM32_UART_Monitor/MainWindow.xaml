<Window x:Class="STM32_UART_Monitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewmodels="clr-namespace:STM32_UART_Monitor.ViewModels"
        Title="STM32 UART Monitor" 
        Height="650" Width="950"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.DataContext>
        <viewmodels:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="STM32 UART Monitor" FontSize="24" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Real-time Serial Communication" FontSize="12" Foreground="White" Opacity="0.9" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Connection Settings -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2" Color="Gray"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Connection Settings" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="COM Port:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding AvailablePorts}"
                                 SelectedItem="{Binding SelectedPort}"
                                 IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"
                                 Height="35" FontSize="13"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="10,0,10,0">
                        <TextBlock Text="Baud Rate:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding BaudRates}"
                                 SelectedItem="{Binding SelectedBaudRate}"
                                 IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"
                                 Height="35" FontSize="13"/>
                    </StackPanel>

                    <Button Grid.Column="2" Content="Refresh" Command="{Binding RefreshPortsCommand}"
                           IsEnabled="{Binding IsConnected, Converter={StaticResource InverseBoolConverter}}"
                           Style="{StaticResource ModernButton}" Margin="10,20,10,0" Width="100" Background="#9E9E9E"/>

                    <Button Grid.Column="3" Content="{Binding ConnectionButtonText}" Command="{Binding ToggleConnectionCommand}"
                           Style="{StaticResource ModernButton}" Margin="10,20,0,0" Width="120"/>
                </Grid>

                <Border Background="#EEEEEE" CornerRadius="4" Padding="10" Margin="0,15,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="12" Height="12" Fill="{Binding IsConnected, Converter={StaticResource StatusColorConverter}}" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding ConnectionStatus}" FontWeight="Bold"/>
                        </StackPanel>

                        <TextBlock Grid.Column="2">
                            <Run Text="Packets: "/><Run Text="{Binding ReceivedPackets}" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}"/>
                        </TextBlock>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>

        <!-- Data Display -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Sensor Values -->
            <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2" Color="Gray"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Sensor Data" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

                    <Border Background="#E3F2FD" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                        <StackPanel>
                            <TextBlock Text="ADC Value" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding AdcValue}" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{StaticResource PrimaryBrush}"/>
                            <TextBlock Text="(0 - 4095)" FontSize="11" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Background="#E0F7FA" CornerRadius="8" Padding="15">
                        <StackPanel>
                            <TextBlock Text="Temperature" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <TextBlock Text="{Binding TemperatureValue}" FontSize="40" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{StaticResource SecondaryBrush}"/>
                            <TextBlock Text="Celsius" FontSize="11" Foreground="Gray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Serial Monitor -->
            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2" Color="Gray"/>
                </Border.Effect>

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <DockPanel Margin="0,0,0,10">
                        <TextBlock Text="Serial Monitor" FontSize="16" FontWeight="Bold"/>
                        <Button Content="Clear" Command="{Binding ClearLogCommand}" HorizontalAlignment="Right"
                               Style="{StaticResource ModernButton}" Background="#F44336" Padding="15,8"/>
                    </DockPanel>

                    <Border Grid.Row="1" Background="#1E1E1E" CornerRadius="4" Padding="10">
                        <ListBox ItemsSource="{Binding LogMessages}" Background="Transparent" Foreground="#00FF00"
                                FontFamily="Consolas" FontSize="12" BorderThickness="0">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="4,2"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>